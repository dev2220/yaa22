export const ACCESS_TOKEN_COOKIE_NAME = 'xs';

export const SERVER_HOST = `${window.location.protocol}//${window.location.host}`;

export const IS_PRODUCTION = process.env.NODE_ENV === 'production';

export const OFFERS_AND_CAMAPIGNS_STEP = 0;
export const AD_SETS_STEP = 1;
